import React from 'react'
import AppTitle from './common/AppTitle';

const DrugInput = () => {
  return (
    <div>
      <AppTitle title={"Input for Drug Hist"}></AppTitle>
    </div>
  );
};

export default DrugInput;